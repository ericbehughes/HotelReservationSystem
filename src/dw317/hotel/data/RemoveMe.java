/*
 * 
 */
package dw317.hotel.data;

/**
 * The Class RemoveMe.
 */
public class RemoveMe {
	/**
	 * This is simply a test class so that we may be able to push this package
	 * to git. If the package remained empty we would not be able to stage the
	 * change in commit and then push it. test
	 **/
}
